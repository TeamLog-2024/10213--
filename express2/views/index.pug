doctype html
html
  head
  body
    h1 사용자 정보 입력
    form(action="/add" method="post")
      div
        | 이름:
        input(type="text" name="name")
      div
        | 나이:
        input(type="number" name="age")
      div
        button(type="submit") 제출
    h2 사용자 목록
    ul
      each user in users
        li
          a(href=`/result/${user.name}`) #{user.name}
          span /
          a(href=`/result/${user.name}`) #{user.age}
